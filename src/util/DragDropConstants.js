/**
 * Drag and Drop constants for Tab Tree
 */

// Drag item types
export const DragItemTypes = {
    TAB: 'TAB',
};

// Drop position indicators
export const DropPosition = {
    BEFORE: 'before',    // Drop before target (as sibling)
    INSIDE: 'inside',    // Drop inside target (as child)
    AFTER: 'after',      // Drop after target (as sibling)
};
